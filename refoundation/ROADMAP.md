# HISTORICAL REFOUNDATION ROADMAP — SUPERSEDED

The original August bootstrap roadmap is preserved verbatim at:

`docs/to-be-deleted/replaced-content/refoundation-ROADMAP.pre-cleanup.md`

It is historical sequence evidence, **not current phase state or task authority**. In particular, its old `P0.2 NEXT`, `P0.3 BLOCKED`, and `P0.5+ NOT AUTHORIZED` labels were superseded by later durable refoundation work.

For current state use:

1. GitHub Issue #1 — latest valid `K` (`h` = accepted head, `a` = active task);
2. `refoundation/ACTIVE-PHASE.yaml` — compact replaceable phase context;
3. `refoundation/SESSION-PROTOCOL.md` — current Manager/Worker protocol;
4. `refoundation/CAPTAIN-DIRECTION.md` — durable Captain direction.

Do not derive a next task from this roadmap's chronology. Current task selection is Issue #1 latest `K -> active T` only.
