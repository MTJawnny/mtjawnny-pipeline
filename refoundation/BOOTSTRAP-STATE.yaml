schema: mtj-refoundation-bootstrap-state/1
status: SUPERSEDED

# Historical compatibility pointer only. This file is retained because older
# durable provenance references its path. It is NOT startup authority and it
# deliberately carries no mirrored phase/task state.

current_state_pointer: GitHub Issue #1 latest K -> active T
manager_entrypoint: refoundation/README.md
worker_contract_pointer: CLAUDE.md

note: >-
  The former bootstrap state remains in Git history. Do not restore it, do not
  derive current state from this tombstone, and do not use dated handoffs or
  filename order as routing. Current accepted head and active task selection
  are read from Issue #1 latest K -> active T.
