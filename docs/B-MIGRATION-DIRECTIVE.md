# ARCHIVED — DO NOT FOLLOW

**Current task authority is GitHub Issue #1: latest `K` -> active `T`.**

Historical body: `archive/routing/docs/B-MIGRATION-DIRECTIVE.md` (original blob `213206b6560d9f097ae86af1673b4630a1c1a64a`, preserved byte-for-byte).

The historical `/1 -> /2` migration is complete. This path remains only as an inert compatibility pointer for old links and for mechanically preserved non-target ruling references. The extracted member-add mutation law now lives in `docs/MEMBER-ADD-MUTATION-LAW.md`. Nothing in this stub assigns work or authorizes a migration.

## Registry-preserved historical ruling-reference lines

The exact original source lines below are retained mechanically so the ruling-registry topology does not fabricate loss of unrelated law during archival.

§10, A1–A15). Captain-ratified 2026-08-01. ZERO API SPEND — all local
"foundry-codebook/2"; `member_oracle_ids` renamed `members` (A1/CDR-11);
## 3. Independent verifier (A13 / B-02 — the audit's core demand)
