# S10–S16 Current Plan — 2026-09-13

Status: **CURRENT PLANNING ROUTER / IMPLEMENTATION AUTHORITY REMAINS GITHUB ISSUE #1**

This document replaces the stale Objective-1–5 pickup/planning routers that were intentionally **not** transplanted onto this clean lineage. It records the current architecture implications of completed preflight and cleanup work. It does not itself authorize implementation, merge, default-branch movement, AQ4 resumption, Bridge v0, Step6, deployment, or publication.

## Durable authority

Always resolve GitHub Issue #1 latest valid `K` and its active `T`. Repository prose is supporting architecture/evidence, never a substitute for durable task selection.

Current planning base descends from the accepted cleanup/conservation implementation head:

`493483aa1d54210c35e1665686a5e0136588da6d`

Standing controls remain:

- AQ4: **PAUSED**
- Bridge v0: **PARKED_UNUSED**
- Step6: **NO**
- merge/default-branch movement: **NO unless separately authorized**

## Completed evidence retained on this lineage

The old five-objective execution routers/prompts are historical and were not copied here. Their completed evidence was transplanted without the obsolete cleanup topology:

- Objective 1 / S10 census and ownership evidence: `docs/architecture/preflight/` and `docs/architecture/preflight/objective1/`
- Objective 2 / S16A adversarial gold suite: `docs/architecture/preflight/s16a/`
- Objective 3 / S16A parser-seam audit: same S16A evidence directory
- Objective 4 / S15 deletion-readiness evidence: `docs/architecture/preflight/s15/`
- Objective 5 / S16B gameplay-DNA research and closure: `docs/architecture/preflight/s16b/`

Historical plans that said Objective 3 was NEXT, Objectives 4–5 were PLANNED, or required a fresh Claude session are not current routing.

## Migration sequence

The engineering migration order remains:

**S10 → S11 → S12 → S13 → S14 → S15**

S16A and S16B are post-migration semantic/product work and do not authorize pulling later semantic changes into the migration slices.

### S10 — dissolve `foundry_common` under conservation

The original Objective-1 ownership conclusions remain applicable, but its importer census was measured at the former S9 accepted head. Cleanup retired the live re-audit packet generator and preserved its original only under `archive/routing/`.

The refreshed census therefore distinguishes live from archive:

- **86 live Python importers** of `foundry_common`;
- **1 archived historical importer** (`archive/routing/experiments/foundry_build_reaudit_packet.py`);
- **87 if archive history is incorrectly counted as implementation work**;
- **35 live externally referenced provider names** when direct, attribute, `getattr`, and string-bound provider contracts are included;
- zero Python parse errors in the refresh audit.

The retired re-audit generator must not be migrated as a live caller. The five frozen AQ4 importers remain byte-unchanged unless separate authority permits their migration.

S10 must preserve the S7 call-time provider substitution behavior and the explicit neutral-vs-DET representation boundary. It must not activate R8.3 or broaden AG-CLI-01.

### S11 — codebook/store/membership

S11 now has two canonical law inputs that did not exist as clean standalone homes when the old migration roadmap was written:

- `docs/MEMBER-ADD-MUTATION-LAW.md` — **AG-CLI-01**;
- `docs/ACTIVATED-REGENERATE-SELF-DET-LAW.md` — **R8.3**.

AG-CLI-01 requires validation, backup, assertion merge without overwrite, history append, lint, atomic write, final SHA-256, and the DET-axis restriction. The law does **not** authorize a new installed mutating CLI.

R8.3 requires that `rule:activated-regenerate-self` remain inactive until a Captain-ratified DET pattern is actually installed. Historical pattern/sample/generator evidence is not activation authority.

### S12 — authority / transport split

The core S12 architecture is unchanged. Preserve selector, immutable transport, verified restore, and negative-control behavior. Archived handoffs or historical routing must never become an authority source again.

### S13 — operator / CLI / command repair

Actual live operator/command surfaces remain in scope, including `.claude/commands/triage-*` legacy module/path references identified by Objective 4.

The retired re-audit packet generator is **not** a migration target. AG-CLI-01 is a conservation law for member-add behavior, not permission to expose a new installed mutating command.

### S14 — AQ4 freeze relocation

AQ4 remains PAUSED. Cleanup changed its document topology intentionally:

- live `docs/AQ4-SEMANTIC-ARCHITECTURE-IMPLEMENTATION-CONTRACT.md` is an inert paused compatibility pointer;
- the full frozen historical contract is byte-preserved at `archive/routing/docs/AQ4-SEMANTIC-ARCHITECTURE-IMPLEMENTATION-CONTRACT.md`.

Frozen AQ4 scripts still contain references to the former direct contract path. Do **not** repair those references piecemeal before S14. S14 must move/freeze code, contract, section references, precommitment, holdout, projection/binding truth, and provenance as one conservation transaction while AQ4 stays paused.

### S15 — retirement / archive / delete

Objective-4 evidence remains useful but is not deletion authority and is now partly superseded by completed cleanup:

- the old `BLOCKED_UNKNOWN` classification for `experiments/foundry_build_reaudit_packet.py` is superseded: the live path is a halt-loud tombstone and its original is byte-preserved in archive;
- blocker B12's claim that `B-MIGRATION-DIRECTIVE.md` is the sole home of AG-CLI-01 is superseded by `docs/MEMBER-ADD-MUTATION-LAW.md`;
- R8.3 likewise has a canonical extracted home;
- registry/archive routing guards created during closeout become part of the later S15 conservation proof.

The **entire S15 readiness matrix must still be regenerated/revalidated at the actual accepted post-S14 head**. No old `DELETE_SAFE`, blocked, or archive classification is self-authorizing.

### S16A — lossless card reading / parser ownership

Use the precommitted Objective-2 gold/holdout evidence unchanged. Objective 3 measured the current seams rather than guessing them. Key current evidence includes:

- full face projection duplicate: exact over 32,557 Gate-0 cards / 33,393 face records;
- neutral self-reference peer: exact over Gate 0;
- neutral vs DET self-reference representation: **1,818 divergent cards**, an intentional boundary rather than a safe collapse;
- locality modal heuristic: **69 genuine mode lines missed / 28 nonmodal lines invented**;
- DET modal expansion: **1,821 / 1,824** certified mode lines, missing the three Tiered modes on *Vincent's Limit Break*;
- reminder-only current assignments: **167 across 13 axes**;
- quote-only assignments after reminder removal: **77 across 25 axes / 72 cards**;
- unresolved S3 contracts: payment timing, conditions/restrictions, source/destination zones, quantity/cardinality, and residual CR/local heuristic islands.

Low-risk implementation order remains: exact S1 duplicate cleanup → preserve explicit S2 representation boundaries → canonical modal recognition → reminder/quote ownership gating → only then select typed contracts for the unresolved S3 concepts.

### S16B — gameplay DNA / functional thesaurus

Objective 5 materially strengthened the prerequisite boundary. S16B semantic freeze requires **both**:

1. accepted S16A lossless mechanical/semantic substrate; and
2. **Objective 6 — ratified Foundry functional vocabulary specification**.

Community terms are discovery evidence, not production semantic authority. Foundry owns the final deterministic definitions. Multi-label membership is mandatory; no forced `primary_role` assumption is allowed merely for storage/ranking convenience.

Current labels such as `ramp`, `cantrip`, `card advantage`, `value`, `stax`, and similar community terms remain preliminary research labels until Objective 6 resolves their exact project definitions, boundaries, aliases, parent/child relations, predicates, thresholds, context requirements, positives, hard negatives, edge cases, and provenance.

## Stale-reference disposition from the 2026-09-13 sweep

The clean planning lineage contains none of the old live preflight routers:

- `PREFLIGHT-PICK-UP-HERE.md`: absent;
- `PREFLIGHT-OBJECTIVES-1-5-2026-09-11.md`: absent;
- `OBJECTIVE-3-FRESH-CHATGPT-PREFLIGHT-PROMPT.md`: absent.

Current-machine W6 provenance and invocation were repaired to the byte-exact archived W6 body and the moved Gate-2 path.

The family-sweep guard's own moved-path metadata was also repaired: its documentation and `generated_by` now identify `tests/guards/gate2/foundry_family_sweep.py`, and its ratified config references point to `config/semantic/`.

Remaining matches to old T3/work-packet paths are historical/provenance records, inert compatibility pointers, or closeout guards. Remaining AQ4 direct-contract references inside frozen benchmark material are intentionally deferred to S14.

## Next implementation boundary

S10 may be selected only by an Issue-#1 task tied to the exact accepted planning/refresh head. S10 implementation must not begin from the old 87-live-importer assumption and must not count archived code as migration work.
