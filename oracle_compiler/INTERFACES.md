# Oracle Compiler Interfaces

`oracle-compiler-interface/0`

Nothing compiler-specific is frozen beyond Captain-ratified V1 and existing repository law.

Implementation tasks must pin `interface_version` and `interface_sha` in their Issue #1 `T` once a nonzero interface exists.

`interface/1` is proposed only after Wave-1 review.

This file is not task state. M00 defines no occurrence, reference, or coverage schema here.
